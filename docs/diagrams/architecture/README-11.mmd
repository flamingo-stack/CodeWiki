classDiagram
    class FileManager {
        +ensure_directory(path)
        +save_json(data, filepath)
        +load_json(filepath)
        +save_text(content, filepath)
        +load_text(filepath)
    }
